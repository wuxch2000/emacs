;;;
;; (add-to-list 'load-path (concat emacs-site-lisp-dir "remember"))

;; (require 'remember)

;; (add-hook 'remember-mode-hook 'org-remember-apply-template)

;; ;; (global-set-key [(f11)(R)]   'remember)
;; (global-set-key [(f6)]   'remember-region)

(provide 'wuxch-remember)
